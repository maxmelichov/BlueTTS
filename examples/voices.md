# Voices

You can test different voices using the main app script. Here are some one-liner invocations:

```bash
uv run python examples/app.py --lang he --voice female1 --text "שלום"
uv run python examples/app.py --lang en --voice female1 --text "Hello"
```
